[project]
name = "adwaitha-hello"
version = "0.1.0"
description = "Demo: say hello to an argument or 'world' by default"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
[project.scripts]
adwaitha-hello="adwaitha_hello.__main__:main"

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"
