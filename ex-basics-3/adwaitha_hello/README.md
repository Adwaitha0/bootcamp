## ex-basics-3 : CLI

Build and install locally:

```bash
uv build
uv venv .venv
source .venv/bin/activate
pip install dist/adwaitha_hello-0.2.0-py3-none-any.whl
```
Run:

```bash
adwaitha-hello --name hello
adwaitha-hello --name Alice
```
